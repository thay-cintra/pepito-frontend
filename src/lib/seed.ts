/**
 * Seed local da Pepito intencionalmente VAZIO.
 *
 * Anteriormente havia 3 análises fictícias para demonstração; foram removidas
 * para não poluir a Fila de Revisão com dados que não vêm da Fila PLD real do
 * Retool (`squad_core.registration_notebook_output_single`).
 *
 * Se quiser repovoar para testes, adicione objetos `Analise` ao array abaixo.
 */
export function seedIfEmpty() {
  // intencionalmente vazio — sem fixtures.
}
