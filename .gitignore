node_modules
dist
.DS_Store
*.local
.vite

# PII — dados reais da Fila PLD (não commitar)
src/data/registration-queue-real.json
.tools/node/
.tools/*.log

# Credenciais e segredos
.env
certs/

# Lock files transientes (fcntl.flock) do merge-write do ledger Credilink-PEP
# — sincronização entre processos concorrentes, sem valor em git.
*.json.lock
